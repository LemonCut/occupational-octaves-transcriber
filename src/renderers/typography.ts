/**
 * Occupational Octaves Typography & Diacritic Configuration
 * 
 * You can adjust font sizes and diacritic dimensions here.
 * Changes apply automatically to both the SVG preview and the PDF export.
 */
export const FONT_METRICS = {
  /** Font size for a single note in a half-cell */
  singleNote: 24,

  /** Font size for each note in a 2-note chord (dyad) */
  dyadNote: 17,

  /** Font size for each note in a 3-note chord (triad) */
  triadNote: 15,

  /** Font size for '*' and '+' octave diacritic markers */
  diacriticSize: 14,

  /** Width and height in points for the '▪' octave square marker */
  squareSize: 6,

  /** Radius in points for the black sharp (•) and flat (•) accidental dots */
  dotRadius: 2.5
};
